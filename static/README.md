# Static Files Directory

This directory contains static files for the YouTube Content Detection System.

## Structure:
- `css/` - Custom stylesheets
- `js/` - JavaScript files
- `images/` - Image assets (logos, icons, etc.)

## Usage:
Static files are served using Django's static file system.
Run `python manage.py collectstatic` before deployment.
